name: "if conditions practice"
on: 
   pull_request:
    branches:
      - main
      - master


jobs: 
  deploy:
    runs-on: ubuntu-latest
    if:   github.ref == 'refs/heads/main' 

    steps:
      - name: "testing"
        run: |
               [[ 1 -ne 1 ]] 
      - name: "print value"
        run : echo "==end=="


